# تفعيل إعادة الكتابة
RewriteEngine On

# إذا كانت الصفحة الرئيسية أو غير موجودة، أعد التوجيه إلى admin/login.php
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ /admin/login.php [L]

# السماح بالوصول للملفات والمجلدات الفعلية
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
